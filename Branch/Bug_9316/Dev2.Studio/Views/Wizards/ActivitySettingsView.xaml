<UserControl x:Class="Dev2.Studio.Views.ActivitySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"                     
             xmlns:local="clr-namespace:Dev2.Studio.AppResources.Behaviors"
             xmlns:cs="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
    Background="White" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding WizardTitle}" Margin="5,5,0,0" VerticalAlignment="Center" />
        <Button Grid.Row="0" Content="X" Command="{Binding CancelCommand}" AutomationProperties.AutomationId="UI_ActivitySettingsCancelBtn_AutoID" 
                Margin="0,5,5,0" HorizontalAlignment="Right" VerticalAlignment="Center" />

        <!--<Border Grid.Row="1" HorizontalAlignment="Stretch" BorderBrush="Black" BorderThickness="0,0,0,1" Margin="5,5,5,0" />-->

        <Border BorderBrush="DarkGray" BorderThickness="2"
                Grid.Row="2" Visibility="{Binding ShowConnectPrompt, Converter={StaticResource BoolToVisibilityConverterNegative}}" Margin="5">
            <cs:WebView x:Name="browser">
                <cs:WebView.ContextMenu>
                    <ContextMenu />
                </cs:WebView.ContextMenu>
                <i:Interaction.Behaviors>
                    <local:WebControlBrowseOnEventBehaviour BrowseRequestedEventName="BrowseRequested" />
                </i:Interaction.Behaviors>
            </cs:WebView>
        </Border>

        <Border BorderBrush="DarkGray" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" BorderThickness="2"
                Grid.Row="2" Visibility="{Binding ShowConnectPrompt, Converter={StaticResource BoolToVisibilityConverterPositive}}" Margin="5">
            <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <TextBlock TextWrapping="Wrap" TextAlignment="Center" FontWeight="Bold" FontSize="20" Text="The server for this wizard is unavailable." />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Try Connect" Margin="0,0,0,0" Command="{Binding TryConnectCommand}" AutomationProperties.AutomationId="UI_ActivitySettingsTryConnectBtn_AutoID" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
