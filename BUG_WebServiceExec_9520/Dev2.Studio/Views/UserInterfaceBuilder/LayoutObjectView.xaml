<UserControl x:Class="Unlimited.Applications.BusinessDesignStudio.LayoutObjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel MouseDown="StackPanel_MouseDown">
        <StackPanel.ContextMenu>
            <ContextMenu>
                <Separator />
                <MenuItem Header="Copy" Command="{Binding CopyCommand}" AutomationProperties.AutomationId="UI_LayoutObjectCopyMenuItem_AutoID" />
                <MenuItem Header="Cut" Command="{Binding CutCommand}" AutomationProperties.AutomationId="UI_LayoutObjectCutMenuItem_AutoID" />
                <MenuItem Header="Paste" Command="{Binding PasteCommand}" AutomationProperties.AutomationId="UI_LayoutObjectPasteMenuItem_AutoID" />
                <Separator />
                <MenuItem Header="Add Row Above" Command="{Binding AddRowAboveCommand}" AutomationProperties.AutomationId="UI_LayoutObjectAddRowAboveMenuItem_AutoID" />
                <MenuItem Header="Add Row Below" Command="{Binding AddRowBelowCommand}" AutomationProperties.AutomationId="UI_LayoutObjectAddRowBelowMenuItem_AutoID" />
                <MenuItem Header="Add Column To The Left" Command="{Binding AddColumnLeftCommand}" AutomationProperties.AutomationId="UI_LayoutObjectAddColumnLeftMenuItem_AutoID" />
                <MenuItem Header="Add Column To The Right" Command="{Binding AddColumnRightCommand}" AutomationProperties.AutomationId="UI_LayoutObjectAddColumnRightMenuItem_AutoID" />
                <MenuItem Header="Delete Row" Command="{Binding DeleteRowCommand}" AutomationProperties.AutomationId="UI_LayoutObjectDeleteRowMenuItem_AutoID" />
                <MenuItem Header="Delete Column" Command="{Binding DeleteColumnCommand}" AutomationProperties.AutomationId="UI_LayoutObjectDeleteColumnMenuItem_AutoID" />
                <MenuItem Header="Clear Cell Content" Command="{Binding DeleteCellCommand}" AutomationProperties.AutomationId="UI_LayoutObjectClearCellMenuItem_AutoID" />
                <MenuItem Header="Clear All Cells" Command="{Binding ClearAllCommand}" AutomationProperties.AutomationId="UI_LayoutObjectClearAllMenuItem_AutoID" />
            </ContextMenu>
        </StackPanel.ContextMenu>
        <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter Property="Background" Value="LightBlue" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsSelected}" Value="False">
                            <Setter Property="Background" Value="LightGray" />
                        </DataTrigger>
                    </Style.Triggers>

                </Style>
        </StackPanel.Style>
        <Grid IsHitTestVisible="False">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="35" />
                <RowDefinition Height="35"/>
            </Grid.RowDefinitions>
                
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40" />
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Image Width="35" Height="35" Margin="3,0" Source="{Binding IconPathObject, Converter={StaticResource LayoutObjectViewModelToImageConverter}}" />
            <TextBlock Text="{Binding WebpartServiceDisplayName}" MaxWidth="110" MaxHeight="35" Grid.Column="1" FontWeight="Bold" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" FontSize="13" />
            <TextBlock Grid.ColumnSpan="2" Grid.Row="1" MaxWidth="110" MaxHeight="35" Text="{Binding DisplayName}" HorizontalAlignment="Center" TextWrapping="Wrap" TextTrimming="CharacterEllipsis">

            </TextBlock>
        </Grid>
    </StackPanel>
</UserControl>
