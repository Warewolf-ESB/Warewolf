<UserControl x:Class="Warewolf.Studio.CustomControls.PasswordBoxUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="clr-namespace:Dev2.UI;assembly=Dev2.Intellisense"
             xmlns:languages="clr-namespace:Warewolf.Studio.Resources.Languages;assembly=Warewolf.Studio.Resources"
             mc:Ignorable="d">
    
    <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
    <ui:IntellisenseTextBox Grid.Column="0"
                            Grid.Row="0"
                            Name="txtVisiblePassword"
                            LostFocus="TxtPassword_OnLostFocus"
                            Visibility="Hidden"
                            Margin="{StaticResource ActivityBorderTop}"
                            ToolTip="{x:Static languages:Tooltips.FileActivityToolTipPassword}">
    </ui:IntellisenseTextBox>
        <PasswordBox Grid.Column="0"
                     Grid.Row="0"
                     Name="txtPassword"
                     LostFocus="TxtPassword_OnLostFocus"
                     Visibility="Visible"
                     Margin="{StaticResource ActivityBorderTop}"
                     ToolTip="{x:Static languages:Tooltips.FileActivityToolTipPassword}">
        </PasswordBox>
        
        <Button Grid.Row="0" Grid.Column="1" Click="ButtonBase_OnClick">
            <Image Name="imgShowHide">
            </Image>
        </Button>
            
    </Grid>
</UserControl>

