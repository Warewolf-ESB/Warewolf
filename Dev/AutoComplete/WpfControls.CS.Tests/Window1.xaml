<Window x:Class="WpfControls.CS.Test.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wpf="http://wpfcontrols.com/"
        xmlns:local="clr-namespace:WpfControls.CS.Test"
        xmlns:editors="clr-namespace:WpfControls.Editors;assembly=WpfControls"
        Title="{Binding Path=SelectedItem, ElementName=Actb}" SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        Padding="10">
    <Window.Resources>
        <local:FilesystemSuggestionProvider x:Key="fsp" />

        <DataTemplate x:Key="fsTemplate">
            <Border Padding="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Image Grid.RowSpan="2"
                           Width="32"
                           Height="32"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Source="{Binding Converter={StaticResource FileIconConverter}}"/>
                    <TextBlock Text="{Binding Path=Name}"
                               FontWeight="Bold" 
                               Grid.Column="1" 
                               Grid.Row="0"
                               FontFamily="Segoe UI"
                               VerticalAlignment="Bottom"/>
                    <TextBlock Text="{Binding Path=LastWriteTime, StringFormat='Last Modified: {0}'}"
                               Grid.Column="1"
                               Grid.Row="1"
                               VerticalAlignment="Top"/>
                </Grid>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="dirTemplate">
            <Border Padding="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Image Width="32"
                           Height="32"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Source="{Binding Converter={StaticResource FileIconConverter}}"/>
                    <TextBlock Text="{Binding Path=Name}"
                               FontWeight="SemiBold" 
                               Grid.Column="1" 
                               Grid.Row="0"
                               FontFamily="Segoe UI"
                               FontSize="14"
                               VerticalAlignment="Center"/>
                    
                </Grid>
            </Border>
        </DataTemplate>

    </Window.Resources>
    <Grid Width="400"
          Margin="20"
          HorizontalAlignment="Center"
          VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Content="level"></Label>
        <TextBox Text="{Binding Level}" Grid.Column="1"></TextBox>
        
        <Label Content="_Open"   Grid.Column="0"
                                 Grid.Row="1" />
        <editors:AutoCompleteTextBox VerticalAlignment="Top"
                                 Height="25"
                                 Grid.Column="1"
                                 Grid.Row="1"

            Watermark="Search here"
                                 IconPlacement="Left"
                                 IconVisibility="Visible"
                                 Provider="{Binding Dev2Provider}"
                                 x:Name="Actb">
            <editors:AutoCompleteTextBox.SelectedItem>
                <Binding Path="SelectedPath" Mode="TwoWay">
                    <Binding.ValidationRules>
                        <local:RequiredValidationRule/>
                    </Binding.ValidationRules>
                </Binding>
            </editors:AutoCompleteTextBox.SelectedItem>
            <editors:AutoCompleteTextBox.LoadingContent>
                <TextBlock Text="Loading..."
                           Margin="5"
                           FontSize="14" />
            </editors:AutoCompleteTextBox.LoadingContent>
            <editors:AutoCompleteTextBox.Icon>
                <Border Width="20"
                        Height="20">
                    <Image Source="Resources/search_icon.png"
                           Height="16"
                           Width="16"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
                </Border>
            </editors:AutoCompleteTextBox.Icon>
        </editors:AutoCompleteTextBox>

        <Label Content="Recset"   Grid.Column="0"
                                 Grid.Row="2" />
        <editors:AutoCompleteTextBox VerticalAlignment="Top"
                                 Height="25"
                                 Grid.Column="1"
                                 Grid.Row="2"

            Watermark="Search here"
                                 IconPlacement="Left"
                                 IconVisibility="Visible"
                                 Provider="{Binding Dev2RecsetProvider}"
                                 x:Name="Act2b">
            <editors:AutoCompleteTextBox.SelectedItem>
                <Binding Path="SelectedPathRecset" Mode="TwoWay">
                    <Binding.ValidationRules>
                        <local:RequiredValidationRule/>
                    </Binding.ValidationRules>
                </Binding>
            </editors:AutoCompleteTextBox.SelectedItem>
            <editors:AutoCompleteTextBox.LoadingContent>
                <TextBlock Text="Loading..."
                           Margin="5"
                           FontSize="14" />
            </editors:AutoCompleteTextBox.LoadingContent>
            <editors:AutoCompleteTextBox.Icon>
                <Border Width="20"
                        Height="20">
                    <Image Source="Resources/search_icon.png"
                           Height="16"
                           Width="16"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
                </Border>
            </editors:AutoCompleteTextBox.Icon>
        </editors:AutoCompleteTextBox>


        <Label Content="Scalar"   Grid.Column="0"
                                 Grid.Row="3" />
        <editors:AutoCompleteTextBox VerticalAlignment="Top"
                                 Height="25"
                                 Grid.Column="1"
                                 Grid.Row="3"

            Watermark="Search here"
                                 IconPlacement="Left"
                                 IconVisibility="Visible"
                                 Provider="{Binding Dev2ScalarProvider}"
                                 x:Name="Actb2">
            <editors:AutoCompleteTextBox.SelectedItem>
                <Binding Path="SelectedPathScalar" Mode="TwoWay">
                    <Binding.ValidationRules>
                        <local:RequiredValidationRule/>
                    </Binding.ValidationRules>
                </Binding>
            </editors:AutoCompleteTextBox.SelectedItem>
            <editors:AutoCompleteTextBox.LoadingContent>
                <TextBlock Text="Loading..."
                           Margin="5"
                           FontSize="14" />
            </editors:AutoCompleteTextBox.LoadingContent>
            <editors:AutoCompleteTextBox.Icon>
                <Border Width="20"
                        Height="20">
                    <Image Source="Resources/search_icon.png"
                           Height="16"
                           Width="16"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
                </Border>
            </editors:AutoCompleteTextBox.Icon>
        </editors:AutoCompleteTextBox>

        <StackPanel Grid.Column="1"
                    Grid.Row="4"
            Margin="0,10"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Content="Open"
                    Command="{Binding Path=OpenCommand}"
                    Padding="20,3" />
            <Button Content="Cancel"
                    Margin="5,0"
                    Command="{Binding Path=CancelCommand}"
                    Padding="20,3" />
        </StackPanel>
    </Grid>
</Window>
