<UserControl x:Class="Dev2.Views.Merge.MergeWorkflowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:customcontrols="clr-namespace:Dev2.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <customcontrols:ActivityTemplateSelector x:Key="ActivitySelector" />
    </UserControl.Resources>

    <Grid Margin="{StaticResource ElementBorder}" x:Name="MainGrid" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60*"/>
            <ColumnDefinition Width="40*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Grid x:Name="MergeCurrentGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Background="{StaticResource WareWolfMenuBrush}" HorizontalAlignment="Stretch">
                    <TextBlock Margin="5" Text="Current" Style="{StaticResource SourceHeader}" />
                </StackPanel>
                <Grid Grid.Row="1">
                    <ListBox Grid.Column="0" x:Name="MergeCurrentListBox" ItemsSource="{Binding CurrentMergeList}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <RadioButton Padding="0,0,5,0" x:Name="MergeCurrentRB" IsChecked="{Binding IsMergeCurrentChecked}">
                                    </RadioButton>
                                    <Grid Grid.Column="1">
                                        <ContentPresenter x:Name="ControlContentPresenter" ContentTemplateSelector="{StaticResource ActivitySelector}">

                                        </ContentPresenter>
                                    </Grid>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Grid>
            <Grid Grid.Column="1" x:Name="MergeDifferenceGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Background="{StaticResource WareWolfMenuBrush}" HorizontalAlignment="Stretch">
                    <TextBlock Margin="5" Text="Difference" Style="{StaticResource SourceHeader}" />
                </StackPanel>
                <Grid Grid.Row="1">
                    <ListBox Grid.Column="0" x:Name="MergeDifferenceListBox" ItemsSource="{Binding CurrentMergeList}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <RadioButton Padding="0,0,5,0" x:Name="MergeCurrentRB" IsChecked="{Binding IsMergeDifferenceChecked}">
                                    </RadioButton>
                                    <Grid Grid.Column="1">
                                        <ContentPresenter x:Name="ControlContentPresenter" ContentTemplateSelector="{StaticResource ActivitySelector}">

                                        </ContentPresenter>
                                    </Grid>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Grid>
        </Grid>

        <Grid Grid.Column="1" x:Name="MergeGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Background="{StaticResource WareWolfMenuBrush}" HorizontalAlignment="Stretch">
                <TextBlock Margin="5" Text="Merge" Style="{StaticResource SourceHeader}" />
            </StackPanel>
            <Grid Grid.Row="1">
                <ListBox Grid.Column="0" x:Name="MergeListBox">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>

                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
