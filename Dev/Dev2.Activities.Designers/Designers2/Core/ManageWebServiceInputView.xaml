<Window x:Class="Dev2.Activities.Designers2.Core.ManageWebServiceInputView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:languages="clr-namespace:Warewolf.Studio.Resources.Languages;assembly=Warewolf.Studio.Resources"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:core="clr-namespace:Warewolf.Studio.Core;assembly=Warewolf.Studio.Core"
        xmlns:interfaces="clr-namespace:Dev2.Common.Interfaces;assembly=Dev2.Common.Interfaces"
        xmlns:customControls="clr-namespace:Dev2.CustomControls;assembly=Dev2.CustomControls"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Style="{StaticResource WindowBorderlessStyle}"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        mc:Ignorable="d" 
        Width="600"
        Height="450"
        MouseDown="ManageWebServiceInputView_OnMouseDown"
        d:DesignHeight="450" d:DesignWidth="600"
        d:DataContext="{d:DesignInstance Type=interfaces:IManageWebServiceInputViewModel, IsDesignTimeCreatable=False}">
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding Path=CloseCommand}"/>
    </Window.InputBindings>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Warewolf.Studio.Themes.Luna;component/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Background="{StaticResource WareWolfMenuBrush}" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Stretch">
            <Image Margin="5,0,0,0" Width="24" Height="24" Source="{Binding TestIconImageSource}" />
            <TextBlock Margin="{StaticResource ElementBorder}" Text="{x:Static languages:Core.WebGetTestHeader}" Style="{StaticResource SourceHeader}" x:Name="HeaderTextBlock" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="{StaticResource ElementBorder}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Column="0" Grid.Row="0" Text="{x:Static languages:Core.InputsHeader}" FontWeight="Bold" />
                <ScrollViewer Grid.Column="0" Grid.Row="1"  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <ListBox x:Name="InputsList" 
                             Grid.IsSharedSizeScope="True" 
                             ItemsSource="{Binding Inputs}" 
                             BorderBrush="{StaticResource WarewolfPanelGreyBrush}" 
                             MaxHeight="125"
                             HorizontalContentAlignment="Stretch"
                             ItemContainerStyle="{StaticResource ListBoxItemNoBorderStyle}" 
                             Background="{StaticResource WarewolfPanelGreyBrush}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="InputName" 
                                               Text="{Binding Path=Name}" 
                                               TextTrimming="CharacterEllipsis" 
                                               MinWidth="50" 
                                               MaxWidth="200" 
                                               HorizontalAlignment="Stretch" 
                                               ToolTip="{Binding Path=Name}"/>
                                    <TextBox Grid.Column="1" 
                                             Margin="{StaticResource ControlBorderLeft}" 
                                             Text="{Binding Path=Value}" 
                                             HorizontalAlignment="Stretch" 
                                             customControls:WatermarkTextBox.Watermark="Test Data"
                                             ToolTip="{x:Static languages:Core.ManageDbServiceInputValueTooltip}">
                                        <i:Interaction.Behaviors>
                                            <core:HelpSetter Text="{x:Static languages:Core.DatabaseServiceInputsHelp}" DataContext="{Binding}" />
                                        </i:Interaction.Behaviors>
                                    </TextBox>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>
            </Grid>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="{StaticResource ControlBorderTop}">
                <customControls:CircularProgressBar Width="25"
                                                    HorizontalAlignment="Right" 
                                                    Margin="{StaticResource ControlBorderRight}"
                                                    Height="25"
                                                    Padding="2.5"
                                                    Visibility="{Binding Path=IsTesting,Converter={StaticResource BoolToVis}}" />
                <Button Width="25" 
                        Height="25"
                        x:Name="PasteButton"
                        Margin="{StaticResource ControlBorderRight}"
                        Command="{Binding PasteResponseCommand}" 
                        ToolTip="{x:Static languages:Core.ManageWebServicePasteTooltip}"
                        VerticalAlignment="Top">
                    <fa:FontAwesome TextOptions.TextFormattingMode="Display" 
                                    FontSize="16" 
                                    FontWeight="DemiBold" 
                                    Icon="Pencil" 
                                    VerticalAlignment="Top" 
                                    HorizontalAlignment="Center"/>
                    <i:Interaction.Behaviors>
                        <core:HelpSetter Text="{x:Static languages:Core.WebServicePasteHelp}" DataContext="{Binding}" />
                    </i:Interaction.Behaviors>
                </Button>
                <Button x:Name="TestActionButton" 
                        HorizontalAlignment="Right" 
                        Command="{Binding TestCommand}" 
                        Click="TestActionButton_OnClick"
                        ToolTip="{x:Static languages:Core.ManageDbServiceTestActionTooltip}"
                        Width="100">Test
                    <i:Interaction.Behaviors>
                        <core:HelpSetter Text="{x:Static languages:Core.DatabaseServiceTestHelp}" DataContext="{Binding}" />
                    </i:Interaction.Behaviors>
                </Button>
            </StackPanel>

            <Grid x:Name="OutputsGrid" Grid.Row="2" Visibility="{Binding TestResultsAvailable,Converter={StaticResource BoolToVis}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{x:Static languages:Core.OutputsLabel}" FontWeight="Bold" />
                <ScrollViewer Grid.Row="1" 
                              Grid.Column="0" 
                              VerticalAlignment="Top"
                              HorizontalScrollBarVisibility="Auto" 
                              VerticalScrollBarVisibility="Auto">
                    <Grid Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ScrollViewer}}, Path=ActualWidth}" >
                        <TextBlock Text="{Binding TestResults}" 
                                   TextWrapping="Wrap"
                                   HorizontalAlignment="Stretch" 
                                   VerticalAlignment="Stretch">
                            <i:Interaction.Behaviors>
                                <core:HelpSetter Text="{x:Static languages:Core.PluginServiceTestResultsHelp}" DataContext="{Binding}" />
                            </i:Interaction.Behaviors>
                        </TextBlock>
                    </Grid>
                </ScrollViewer>
                <TextBlock Grid.Row="1"
                           Text="None" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center" 
                           Visibility="{Binding IsTestResultsEmptyRows,Converter={StaticResource BoolToVis}}" />
            </Grid>

            <Grid x:Name="ResponseGrid" Grid.Row="2" Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Paste Response" FontWeight="Bold" />

                <TextBox Grid.Row="1"
                         Text="{Binding TestResults}" 
                         x:Name="ResponseTextbox"
                         Style="{StaticResource StretchedTextBoxStyle}"
                         customControls:WatermarkTextBox.Watermark="Paste response here"
                         SnapsToDevicePixels="True" 
                         VerticalScrollBarVisibility="Auto"
                         ToolTip="{x:Static languages:Core.ManageWebPasteResponseTooltip}"
                         HorizontalScrollBarVisibility="Disabled" >
                    <i:Interaction.Behaviors>
                        <core:HelpSetter Text="{x:Static languages:Core.WebServicePasteInputHelp}" DataContext="{Binding}" />
                    </i:Interaction.Behaviors>
                </TextBox>
            </Grid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="{StaticResource ControlBorderTop}" HorizontalAlignment="Right">
                <Button x:Name="DoneButton" 
                        HorizontalAlignment="Right" 
                        VerticalAlignment="Bottom"
                        Margin="{StaticResource ControlBorderRight}"
                        Command="{Binding OkCommand}"
                        Width="100"
                        Content="Done"/>
                <Button HorizontalAlignment="Right" 
                        VerticalAlignment="Bottom"
                        Command="{Binding CloseCommand}"
                        Width="100"
                        Content="Cancel"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>