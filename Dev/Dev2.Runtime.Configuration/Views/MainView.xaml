<UserControl x:Class="Dev2.Runtime.Configuration.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" MinWidth="250"/>
            <ColumnDefinition Width="8*"/>
        </Grid.ColumnDefinitions>
       
        <StackPanel Grid.Column="0">
            <TreeView Name="Tree" Grid.Column="0" ItemsSource="{Binding SettingsObjects}" SelectedItemChanged="TreeView_OnSelectedItemChanged"
                      Height="300">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="{x:Type TreeViewItem}">
                        <Setter Property="IsSelected" Value="{Binding Path=IsSelected, Mode=TwoWay}"/>
                    </Style>
                </TreeView.ItemContainerStyle>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding Object.DisplayName}" />
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
            <Grid>
                <Button HorizontalAlignment="Left" Content="Cancel" Command="{Binding CancelCommand}" Margin="0,3,0,0" Width="50" 
                        AutomationProperties.AutomationId="UI_SettingsCancelBtn" IsEnabled="{Binding Configuration.HasChanges}"/>
                <Button HorizontalAlignment="Right" Content="Save" Command="{Binding SaveCommand}" Margin="0,3,0,0" Width="50" 
                        AutomationProperties.AutomationId="UI_SettingsSaveBtn" IsEnabled="{Binding Configuration.HasChanges}"/>
            </Grid>
        </StackPanel>
        
        <ContentControl Grid.Column="1" Content="{Binding SettingsView}" Margin="3,0,0,0"  />

        <Border Grid.ColumnSpan="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="White"
                Visibility="{Binding ErrorsVisible}">
            <StackPanel>
                <TextBlock Text="Errors" FontSize="20" />
                <ScrollViewer Height="100" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Errors}" Margin="0,5,0,0" />
                </ScrollViewer>
                <Button Content="Close" Command="{Binding ClearErrorsCommand}" HorizontalAlignment="Left" Width="50" Margin="0,3,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
