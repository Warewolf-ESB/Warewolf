<UserControl x:Class="Warewolf.Studio.Views.ServerUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:ig="http://schemas.infragistics.com/xaml"
              xmlns:converters="clr-namespace:Warewolf.Studio.Views.Converters"
              xmlns:views="clr-namespace:Warewolf.Studio.Views"
              mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>

    </UserControl.Resources>
    <Grid>
        
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="200"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"></RowDefinition>
                    <RowDefinition Height="50"></RowDefinition>
                    <RowDefinition Height="50"></RowDefinition>
                    <RowDefinition Height="50"></RowDefinition>
                    <RowDefinition Height="50"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
                <TextBlock Text="{Binding Path= ServerLabel}" Grid.Column="0"></TextBlock>
                <TextBox Text="{Binding Server}" Grid.Column="1" Grid.Row="0"></TextBox>
                
                <TextBlock Text="{Binding Path=AuthenticationTypeText}" Grid.Column="0" Grid.Row="1"></TextBlock>
                <Grid Grid.Column="1" Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                <RadioButton Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" IsChecked="{Binding Path=AuthenticationType, Converter=EnumToBoolConverter }"></RadioButton>
                <RadioButton Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"></RadioButton>
                <RadioButton Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center"></RadioButton>
            </Grid>
                
                <TextBlock Text="{Binding Path=UserLabel}" Grid.Column="0" Grid.Row="1"></TextBlock>
                <TextBox Text="{Binding UserName}" Grid.Column="1" Grid.Row="2"></TextBox>
                
                <TextBlock Text="{Binding Path= PasswordLabel}" Grid.Row="3" Grid.Column="1"></TextBlock>
                <TextBox Text="{Binding Password}" Grid.Column="1" Grid.Row="3"></TextBox>
        </Grid>
    </Grid>
</UserControl>
