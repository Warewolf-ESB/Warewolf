<UserControl x:Class="Warewolf.Studio.Views.ConnectControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:interfaces="clr-namespace:Dev2.Common.Interfaces;assembly=Dev2.Common.Interfaces"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=interfaces:IConnectControlViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="29" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="30" />
            <ColumnDefinition Width="30" />
        </Grid.ColumnDefinitions>

        <ComboBox Grid.Row="0"
                  Grid.Column="0"
                  ItemsSource="{Binding Servers}"
                  SelectedItem="{Binding SelectedConnection}"
                  >
            <ComboBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ComboBox.ItemsPanel>
        </ComboBox>


        <Button Grid.Row="0"
                Grid.Column="1"
                Width="25"
                Height="25"
                Command="{Binding EditConnectionCommand}"
                Padding="4"
                Style="{StaticResource ExplorerButtonStyle}">
            <Image Source="{StaticResource System-More-Inverted}" />
        </Button>
        <Button Grid.Row="0"
                Grid.Column="2"
                Width="25"
                Height="25"
                Command="{Binding ToggleConnectionStateCommand}"
                Padding="4"
                Style="{StaticResource ExplorerButtonStyle}">
            <Image Source="{StaticResource System-Link-Inverted}" />
        </Button>
    </Grid>

</UserControl>
