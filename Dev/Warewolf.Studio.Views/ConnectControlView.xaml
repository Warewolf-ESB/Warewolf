<UserControl x:Class="Warewolf.Studio.Views.ConnectControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:interfaces="clr-namespace:Dev2.Common.Interfaces;assembly=Dev2.Common.Interfaces"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance Type=interfaces:IConnectControlViewModel,
                                              IsDesignTimeCreatable=False}">
    <StackPanel Orientation="Horizontal">
        <ComboBox ItemsSource="{Binding Servers}" SelectedItem="{Binding SelectedConnection}"/>
        <Button Width="25" Style="{StaticResource ExplorerButtonStyle}"
                    Height="25"
                    Padding="4" Command="{Binding EditConnectionCommand}">
            <Image Source="{StaticResource System-More-Inverted}" />
        </Button>
        <Button Width="25" Style="{StaticResource ExplorerButtonStyle}"
                    Height="25"
                    Padding="4" Command="{Binding ToggleConnectionStateCommand}">
            <Image Source="{StaticResource System-Link-Inverted}" />
        </Button>
    </StackPanel>
</UserControl>
