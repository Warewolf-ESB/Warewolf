<ig:XamDialogWindow x:Class="Warewolf.Studio.Views.RequestServiceNameView"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:fa="http://schemas.fontawesome.io/icons/"
                    xmlns:ig="http://schemas.infragistics.com/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:viewModels="clr-namespace:Warewolf.Studio.ViewModels;assembly=Warewolf.Studio.ViewModels"
                    xmlns:views="clr-namespace:Warewolf.Studio.Views"
                    xmlns:saveDialog="clr-namespace:Dev2.Common.Interfaces.SaveDialog;assembly=Dev2.Common.Interfaces"
                    Width="700"
                    Height="420"
                    CloseButtonVisibility="Collapsed"
                    MaximizeButtonVisibility="Collapsed"
                    MinimizeButtonVisibility="Collapsed"
                    HeaderIconVisibility="Collapsed"
                    Header="Please enter a service name"
                    Style="{StaticResource XamDialogWindowStyle}"
                    mc:Ignorable="d"
                    d:DataContext="{d:DesignInstance Type=saveDialog:IRequestServiceNameViewModel,
                                              IsDesignTimeCreatable=False}">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <!--  HEADER PANEL  -->
<!--        <TextBlock Grid.Row="0"-->
<!--                           Margin="5"-->
<!--                           VerticalAlignment="Stretch"-->
<!--                           Style="{StaticResource SourceHeader}"-->
<!--                           Text="Please enter a service name"-->
<!--                           Visibility="Visible" />-->

        <!--  EMBEDDED CLIENT PANEL  -->
        <views:ExplorerView DataContext="{Binding SingleEnvironmentExplorerViewModel}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch"/>

        <!-- Name Panel -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource TextPanelSubHeaderStyle}" Margin="5,0,0,0">Name:</TextBlock>
            <TextBox Width="300" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="2,0,0,0" Style="{StaticResource TextBoxStyle}"></TextBox>
        </StackPanel>
        <!--  FOOTER PANEL  -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="5" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="24" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Row="1"
                       Grid.Column="1"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center">

                <TextBlock HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Style="{StaticResource ErrorTextBlockStyle}"
                           Text="{Binding Path=ErrorMessage}"
                           TextAlignment="Right" />

                <Button Width="60"
                        Height="30"
                        Margin="5,0,0,0"
                        HorizontalAlignment="Right"
                        Command="{Binding Path=OkCommand}"
                        Content="Save"
                        />

                <Button Width="60"
                        Height="30"
                        Margin="5,0,0,0"
                        HorizontalAlignment="Right"
                        Command="{Binding Path=CancelCommand}"
                        Content="Cancel" />

            </DockPanel>

        </Grid>

    </Grid>
</ig:XamDialogWindow>
