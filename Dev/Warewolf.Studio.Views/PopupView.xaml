<ig:XamDialogWindow x:Class="Warewolf.Studio.Views.PopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ig="http://schemas.infragistics.com/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="200"
                            Width="350"
                            Height="200"
                            CloseButtonVisibility="Collapsed"
                            MaximizeButtonVisibility="Collapsed"
                            MinimizeButtonVisibility="Collapsed"
                            IsResizable="False"
                            IsMoveable="False"
                            StartupPosition="Center"
                    >

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="100"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Image x:Name="MessageImage" Grid.Column="0" Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="32" Height="32"></Image>
            <TextBlock x:Name="MessageText" Grid.Column="1" Grid.Row="0"></TextBlock>
            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                <Button x:Name="OkButton"
                          Content="Ok"
                          Width="50" Margin="10,0,10,5"
                        Height="32"
                        Click="OkButton_OnClick"/>
                <Button x:Name="CancelButton"
                        Width="50"
                        Height="32"
                          Content="Cancel" Margin="10,0,10,5" Click="CancelButton_OnClick"/>
            </StackPanel>
        </Grid>
    
</ig:XamDialogWindow>
