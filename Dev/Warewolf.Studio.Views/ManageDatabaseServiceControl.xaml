<UserControl x:Class="Warewolf.Studio.Views.ManageDatabaseServiceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dialogues="clr-namespace:Dev2.Common.Interfaces.Studio.ViewModels.Dialogues;assembly=Dev2.Common.Interfaces"
             xmlns:db="clr-namespace:Dev2.Common.Interfaces.DB;assembly=Dev2.Common.Interfaces"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=db:IManageDbServiceViewModel,
                                              IsDesignTimeCreatable=False}">
    <Grid>
           <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition> 
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions> 
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Grid.Column="1"> <!-- choose Data Source-->
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" HorizontalAlignment="Left" Orientation="Horizontal">
            <TextBlock  Text="1" ></TextBlock>
            <TextBlock  Text="{Binding DataSourceHeader}" ></TextBlock> <!-- Choose data source header-->
            </StackPanel>
            <Grid Grid.Row="1" HorizontalAlignment="Left" >  <!-- Choose data Source -->
               <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
               <ComboBox Grid.Column="0" ItemsSource="{Binding Sources}" DisplayMemberPath="Name"></ComboBox>
               <Button Grid.Column="1" Command="{Binding EditSourceCommand}" IsEnabled="{Binding CanEditSource}" Content=" ... "></Button>
               <TextBlock Grid.Column="2" Text="{Binding OrLabel}"></TextBlock>
                <Button Grid.Column="3" Content="{Binding NewButtonLabel}"></Button>
            </Grid>
            <StackPanel Grid.Row="2" HorizontalAlignment="Left" Orientation="Horizontal"> <!-- Edit in place Region-->
                <TextBlock  Text="1" ></TextBlock>
                <TextBlock  Text="{Binding DataSourceHeader}" ></TextBlock>
                <!-- Choose data source header-->
            </StackPanel>
        </Grid>
        <Grid Grid.Row="0" Grid.Column="1">
            <!-- Select an Action-->
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" HorizontalAlignment="Left" Orientation="Horizontal">
                <TextBlock  Text="2" ></TextBlock>
                <TextBlock  Text="{Binding DataSourceActionHeader}" ></TextBlock>
                <!-- Choose data source header-->
            </StackPanel>
            <Grid Grid.Row="1" HorizontalAlignment="Left" >
                <!-- Choose data Source Action-->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

            </Grid>
            <StackPanel Grid.Row="2" HorizontalAlignment="Left" Orientation="Horizontal">
                <!-- Edit in place Region-->
                <TextBlock  Text="1" ></TextBlock>
                <TextBlock  Text="{Binding DataSourceHeader}" ></TextBlock>
                <!-- Choose data source header-->
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
