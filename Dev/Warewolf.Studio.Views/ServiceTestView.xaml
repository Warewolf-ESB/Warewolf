<UserControl x:Class="Warewolf.Studio.Views.ServiceTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:core="clr-namespace:Warewolf.Studio.Core;assembly=Warewolf.Studio.Core"
             xmlns:languages="clr-namespace:Warewolf.Studio.Resources.Languages;assembly=Warewolf.Studio.Resources"
             xmlns:ui="clr-namespace:Dev2.UI;assembly=Dev2.Intellisense"
             xmlns:ig="http://schemas.infragistics.com/xaml"
             xmlns:interfaceImplementors="clr-namespace:Dev2.Studio.InterfaceImplementors;assembly=Dev2.Intellisense"
             xmlns:provider="clr-namespace:Dev2.Intellisense.Provider;assembly=Dev2.Intellisense"
             xmlns:interfaces1="clr-namespace:Dev2.Studio.Core.Interfaces;assembly=Dev2.Studio.Core"
             xmlns:customControls="clr-namespace:Warewolf.Studio.CustomControls;assembly=Warewolf.Studio.CustomControls"
             xmlns:converters1="clr-namespace:Dev2.Studio.Core.AppResources.Converters;assembly=Dev2.Studio.Core"
             xmlns:serviceModel="clr-namespace:Dev2.Runtime.ServiceModel.Data;assembly=Dev2.Common.Interfaces"
             xmlns:converters="clr-namespace:Dev2.AppResources.Converters;assembly=Dev2.Studio.Core"
             xmlns:controls="clr-namespace:Dev2.Activities.Designers2.Core.Controls;assembly=Dev2.Activities.Designers"
             xmlns:interfaces="clr-namespace:Dev2.Common.Interfaces;assembly=Dev2.Common.Interfaces"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance interfaces1:IServiceTestViewModel}"
             AutomationProperties.AutomationId="UI_ServiceTestView_AutoID">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Warewolf.Studio.Themes.Luna;component/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters1:NullToVisibilityConverter x:Key="NullToVisibilityConverterNullIsCollapsed" NotNullVisibilityValue="Visible" NullVisibilityValue="Collapsed"/>
            <converters:SimpleEnumToBoolConverter x:Key="EnumToBoolConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="{StaticResource ElementBorder}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock >
                <Hyperlink x:Name="RunAllTestsUrl" 
                           Command="{Binding RunAllTestsInBrowserCommand}" 
                           ToolTip="{x:Static languages:Core.ServiceTestRunAllTestsInBrowserTooltip}"
                           Style="{StaticResource HyperLinkStyle}"
                           NavigateUri="{Binding RunAllTestsUrl,UpdateSourceTrigger=PropertyChanged}" >
                    <TextBlock Text="{Binding RunAllTestsUrl,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HoverUnderlineStyle}"></TextBlock>
                </Hyperlink>
            </TextBlock>

            <TextBlock Grid.Row="0" Grid.Column="1" Text="Passing:" HorizontalAlignment="Right" />
            <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding TestPassingResult}" HorizontalAlignment="Right"/>

            <Button Grid.Row="1" Grid.Column="0" Content="Run All" Width="Auto" HorizontalAlignment="Left" Command="{Binding RunAllTestsCommand}"/>
        </Grid>

        <Border Grid.Row="1"
                BorderBrush="{StaticResource ListBoxItemSelectedBorderBrush}" 
                BorderThickness="0,1,0,0" 
                Margin="{StaticResource ElementBorderTop}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30*"/>
                    <ColumnDefinition Width="70*"/>
                </Grid.ColumnDefinitions>

                <ListBox Grid.Column="0" Style="{StaticResource ListBoxStyle}" 
                         BorderThickness="1,0,1,1"
                         PreviewMouseLeftButtonDown="TestsListbox_OnPreviewMouseLeftButtonDown"
                         
                         ItemsSource="{Binding Tests}" SelectionMode="Single" SelectedItem="{Binding SelectedServiceTest, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" x:Name="TestsListbox">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid HorizontalAlignment="Stretch" 
                                      VerticalAlignment="Stretch" 
                                      Visibility="{Binding IsNewTest,Converter={StaticResource BoolToVisibilityConverterNegative}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="10*"/>
                                        <ColumnDefinition Width="10*"/>
                                        <ColumnDefinition Width="60*"/>
                                        <ColumnDefinition Width="10*"/>
                                        <ColumnDefinition Width="10*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Row="0" Grid.RowSpan="2">
                                        <fa:FontAwesome Icon="Check" Foreground="Green" ToolTip="{x:Static languages:Core.ServiceTestPassedTooltip}" FontSize="20" Visibility="{Binding TestPassed, Converter={StaticResource BoolToVisibilityConverter}}" />
                                        <fa:FontAwesome Icon="Times" Foreground="Red" ToolTip="{x:Static languages:Core.ServiceTestFailingTooltip}" FontSize="20"  Visibility="{Binding TestFailing, Converter={StaticResource BoolToVisibilityConverter}}" />
                                        <fa:FontAwesome Icon="Exclamation" Foreground="OrangeRed" Margin="{StaticResource ControlBorderLeft}" ToolTip="{x:Static languages:Core.ServiceTestInvalidTooltip}" FontSize="20"  Visibility="{Binding TestInvalid, Converter={StaticResource BoolToVisibilityConverter}}" />
                                        <fa:FontAwesome Icon="Question" Foreground="Black" ToolTip="{x:Static languages:Core.ServiceTestPendingTooltip}" FontSize="20" Visibility="{Binding TestPending, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    </StackPanel>

                                    <CheckBox Grid.Column="1" 
                                              Grid.Row="0" 
                                              Grid.RowSpan="2" 
                                              HorizontalAlignment="Left" Style="{StaticResource CheckBoxRoundedStyle}" 
                                              Margin="-2,0,0,0"
                                              ToolTip="{x:Static languages:Core.ServiceTestEnableRunSelectedTestTooltip}"
                                              IsChecked="{Binding Enabled}">
                                        <i:Interaction.Behaviors>
                                            <core:HelpSetter Text="{x:Static languages:Core.ServiceTestEnableRunSelectedTestHelpText}" DataContext="{Binding }" />
                                        </i:Interaction.Behaviors>
                                    </CheckBox>

                                    <TextBlock Grid.Column="2" 
                                               Grid.Row="0" 
                                               Text="{Binding NameForDisplay}" 
                                               TextTrimming="CharacterEllipsis"
                                               MaxWidth="200" 
                                               FontSize="12" 
                                               Margin="0,0,0,-4" 
                                               FontWeight="Bold" />

                                    <TextBlock Grid.Column="2" 
                                               Grid.Row="1" 
                                               Text="{Binding LastRunDate}" 
                                               FontSize="11" 
                                               Margin="0,-4,0,0" 
                                               Visibility="{Binding LastRunDateVisibility, Converter={StaticResource BoolToVisibilityConverter}}" />

                                    <TextBlock Grid.Column="2" 
                                               Grid.Row="1" 
                                               Text="{Binding NeverRunString}" 
                                               FontSize="11" 
                                               Margin="0,-4,0,0"
                                               Visibility="{Binding NeverRunStringVisibility, Converter={StaticResource BoolToVisibilityConverter}}" />

                                    <Button Grid.Column="3" 
                                            Grid.Row="0" 
                                            Grid.RowSpan="2" 
                                            Command="{Binding ElementName=TestsListbox, Path=DataContext.RunSelectedTestCommand }" 
                                            CommandParameter="{Binding .}" 
                                            Visibility="{Binding IsTestRunning, Converter={StaticResource BoolToVisibilityConverterNegative}}"
                                            Style="{StaticResource ButtonRunTestTransparentStyle}" >
                                        <Button.Content>
                                            <fa:FontAwesome Icon="Play" FontSize="18" Background="Transparent"/>
                                        </Button.Content>
                                        <i:Interaction.Behaviors>
                                            <core:HelpSetter Text="{x:Static languages:Core.ServiceTestRunSelectedTestHelpText}" DataContext="{Binding}" />
                                        </i:Interaction.Behaviors>
                                    </Button>

                                    <Button Grid.Column="3" 
                                            Grid.Row="0" 
                                            Grid.RowSpan="2" 
                                            Command="{Binding ElementName=TestsListbox, Path=DataContext.StopTestCommand }" 
                                            CommandParameter="{Binding .}" 
                                            Visibility="{Binding IsTestRunning, Converter={StaticResource BoolToVisibilityConverterPositive}}"
                                            Style="{StaticResource ButtonStopTestTransparentStyle}" >
                                        <Button.Content>
                                            <fa:FontAwesome Icon="Stop" FontSize="18" Background="Transparent"/>
                                        </Button.Content>
                                        <i:Interaction.Behaviors>
                                            <core:HelpSetter Text="{x:Static languages:Core.ServiceTestStopSelectedTestHelpText}" DataContext="{Binding}" />
                                        </i:Interaction.Behaviors>
                                    </Button>

                                    <Button Grid.Column="4" 
                                            Grid.Row="0" 
                                            Grid.RowSpan="2" 
                                            Command="{Binding ElementName=TestsListbox, Path=DataContext.DeleteTestCommand }" 
                                            CommandParameter="{Binding .}" 
                                            IsEnabled="{Binding Enabled, Converter={StaticResource InverseBooleanConverter}}"
                                            Style="{StaticResource ButtonDeleteTestTransparentStyle}" >
                                        <Button.Content>
                                            <fa:FontAwesome Icon="Trash" FontSize="18"/>
                                        </Button.Content>
                                        <i:Interaction.Behaviors>
                                            <core:HelpSetter Text="{x:Static languages:Core.TestsDeleteSelectedTestHelpText}" DataContext="{Binding}" />
                                        </i:Interaction.Behaviors>
                                    </Button>
                                </Grid>
                                <Grid HorizontalAlignment="Stretch" 
                                      VerticalAlignment="Stretch" 
                                      Visibility="{Binding IsNewTest,Converter={StaticResource BoolToVisibilityConverterPositive}}" >
                                    <Button Style="{StaticResource ButtonSchedulerTransparentStyle}" 
                                            HorizontalAlignment="Stretch" 
                                            VerticalAlignment="Stretch" 
                                            Command="{Binding ElementName=TestsListbox, Path=DataContext.CreateTestCommand }"
                                            ToolTip="{x:Static languages:Core.ServiceTestNewTestTooltip}">
                                        <Button.Content>
                                            <Grid>
                                                <Grid.ColumnDefinitions >
                                                    <ColumnDefinition Width="15*"/>
                                                    <ColumnDefinition Width="85*"/>
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <fa:FontAwesome Icon="Plus" FontSize="24" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="1" Text="Create a new Test" FontSize="14"/>
                                            </Grid>
                                        </Button.Content>
                                    </Button>
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <ContentControl Grid.Column="1" 
                                Margin="{StaticResource ElementBorderLeft}" 
                                Visibility="{Binding SelectedServiceTest, Converter={StaticResource NullToVisibilityConverterNullIsCollapsed}}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0">
                            <Hyperlink x:Name="RunSelectedTestUrl" 
                                       Command="{Binding RunSelectedTestInBrowserCommand}" 
                                       ToolTip="{x:Static languages:Core.ServiceTestRunSelectedTestInBrowserTooltip}"
                                       Style="{StaticResource HyperLinkStyle}"
                                       NavigateUri="{Binding SelectedServiceTest.RunSelectedTestUrl,UpdateSourceTrigger=PropertyChanged}" >
                                <TextBlock Text="{Binding SelectedServiceTest.RunSelectedTestUrl,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HoverUnderlineStyle}"></TextBlock>
                            </Hyperlink>
                        </TextBlock>

                        <Grid Grid.Row="1" Margin="{StaticResource ControlBorderTop}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Test Name"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedServiceTest.TestName, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Grid.Row="0" Grid.Column="2" Command="{Binding DuplicateTestCommand}" Margin="{StaticResource ControlBorderLeft}" Style="{StaticResource ServiceTestDuplicateButtonStyle}" 
                                    ToolTip="{Binding SelectedServiceTest.DuplicateTestTooltip}">
                                <fa:FontAwesome FontSize="12" FontWeight="Light" Icon="Clone" />
                                <i:Interaction.Behaviors>
                                    <core:HelpSetter Text="{x:Static languages:Core.ServiceTestDuplicateSelectedTestHelpText}" DataContext="{Binding}" />
                                </i:Interaction.Behaviors>
                            </Button>

                            <TextBlock x:Name="RunAsUserTextBlock" Grid.Row="1" Grid.Column="0" Text="Run As User" FontWeight="Bold"/>
                            <Grid Grid.Column="1"  Grid.Row="2" Margin="0,-3,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <RadioButton ToolTip="{x:Static languages:Core.WindowsAuthenticationToolTip}" 
                                             Padding="0,0,5,0" 
                                             x:Name="WindowsRadioButton" 
                                             VerticalAlignment="Center" 
                                             HorizontalAlignment="Left" 
                                             Margin="{StaticResource ControlBorderRight}"
                                             IsChecked="{Binding SelectedServiceTest.AuthenticationType, Mode=TwoWay, Converter={StaticResource EnumToBoolConverter}, 
                                    ConverterParameter={x:Static serviceModel:AuthenticationType.Windows}}">Windows
                                    <i:Interaction.Behaviors>
                                        <core:HelpSetter Text="{x:Static languages:Core.ServiceTestWindowsHelpText}" DataContext="{Binding}" />
                                    </i:Interaction.Behaviors>
                                </RadioButton>
                                <RadioButton Grid.Column="1" 
                                             x:Name="UserRadioButton" 
                                             ToolTip="{x:Static languages:Core.UserAuthenticationToolTip}" 
                                             Padding="0,0,5,0" 
                                             VerticalAlignment="Center" 
                                             HorizontalAlignment="Left" 
                                             Margin="{StaticResource ControlBorderRight}"
                                             IsChecked="{Binding SelectedServiceTest.AuthenticationType, Mode=TwoWay, Converter={StaticResource EnumToBoolConverter}, 
                                        ConverterParameter={x:Static serviceModel:AuthenticationType.User}}">User
                                    <i:Interaction.Behaviors>
                                        <core:HelpSetter Text="{x:Static languages:Core.ServiceTestUserHelpText}" DataContext="{Binding}" />
                                    </i:Interaction.Behaviors>
                                </RadioButton>
                                <RadioButton Grid.Column="2" 
                                             x:Name="PublicRadioButton" 
                                             ToolTip="{x:Static languages:Core.ServerSourceDialogPublicToolTip}" 
                                             Padding="0,0,5,0" 
                                             VerticalAlignment="Center" 
                                             HorizontalAlignment="Left" 
                                             IsChecked="{Binding SelectedServiceTest.AuthenticationType, Mode=TwoWay, Converter={StaticResource EnumToBoolConverter}, 
                                        ConverterParameter={x:Static serviceModel:AuthenticationType.Public}}">Public
                                    <i:Interaction.Behaviors>
                                        <core:HelpSetter Text="{x:Static languages:Core.ServiceTestPublicHelpText}" DataContext="{Binding}" />
                                    </i:Interaction.Behaviors>
                                </RadioButton>
                            </Grid>
                            <Grid Grid.Column="1" Grid.Row="3" 
                                  x:Name="UserNamePasswordContainer"
                                  Margin="0,-3,0,0"
                                  Visibility="{Binding SelectedServiceTest.UserAuthenticationSelected,Converter={StaticResource BoolToVisibilityConverter}}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="{Binding ElementName=RunAsUserTextBlock, Path=ActualWidth}"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Static languages:Core.UserNameLabel}" ></TextBlock>
                                <TextBox Grid.Row="0" 
                                         Grid.Column="1" 
                                         x:Name="UsernameTextBox"
                                         Text="{Binding SelectedServiceTest.UserName}" 
                                         ToolTip="{x:Static languages:Core.UserNameToolTip}" >
                                    <i:Interaction.Behaviors>
                                        <core:HelpSetter Text="{x:Static languages:Core.ServiceTestUserNameHelpText}" DataContext="{Binding}" />
                                    </i:Interaction.Behaviors>
                                </TextBox>
                                <TextBlock Grid.Row="1" 
                                           Grid.Column="0" 
                                           Margin="{StaticResource ControlBorderTop}"
                                           Text="{x:Static languages:Core.PasswordLabel}" ></TextBlock>
                                <PasswordBox x:Name="PasswordTextBox" 
                                             Grid.Row="1" 
                                             Grid.Column="1"
                                             Margin="{StaticResource ControlBorderTop}"
                                             ToolTip="{x:Static languages:Core.PasswordToolTip}"
                                             customControls:PasswordBoxAssistant.BindPassword="true"
                                             customControls:PasswordBoxAssistant.BoundPassword="{Binding SelectedServiceTest.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                    <i:Interaction.Behaviors>
                                        <core:HelpSetter Text="{x:Static languages:Core.ServiceTestPasswordHelpText}" DataContext="{Binding}" />
                                    </i:Interaction.Behaviors>
                                </PasswordBox>
                            </Grid>
                        </Grid>

                        <Grid Grid.Row="2" Margin="{StaticResource ControlBorderTop}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Inputs" FontWeight="Bold"/>
                            <Border Grid.Row="1" BorderBrush="{StaticResource XamGridBorderBrush}" BorderThickness="1">
                                <controls:Dev2DataGrid ItemsSource="{Binding SelectedServiceTest.Inputs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                       AutoGenerateColumns="False"
                                                       BorderThickness="0"      
                                                       Style="{StaticResource ActivityMapppingDataGridStyle}"
                                                       x:Name="TestInputs">

                                    <!-- DO NOT override ItemsPanel - doing so causes column sizing issues on first drop -->
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Width="*"
                                                                MinWidth="125"
                                                                Header="Variable"
                                                                HeaderStyle="{StaticResource ActivityMapppingDataGridHeaderStyle}"
                                                                d:DataContext="{d:DesignInstance Type=interfaces:IServiceTestInput}">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock KeyboardNavigation.IsTabStop="False" 
                                                               Focusable="True" 
                                                               ToolTip="{Binding Path=Variable}" 
                                                               Text="{Binding Path=Variable, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Width="*"
                                                                MinWidth="125"
                                                                Header="Value"
                                                                HeaderStyle="{StaticResource ActivityMapppingDataGridHeaderStyle}"
                                                                d:DataContext="{d:DesignInstance Type=interfaces:IServiceTestInput}">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ui:IntellisenseTextBox FilterType="All" 
                                                                            WrapInBrackets="False" 
                                                                            Text="{Binding Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                                            Margin="1,0,1,1"
                                                                            AllowMultilinePaste="True"
                                                                            TextWrapping="Wrap"
                                                                            Focusable="True" 
                                                                            TextChanged="TxtValue_OnTextChanged"   
                                                                            IsTabStop="True"
                                                                            AcceptsReturn="True"
                                                                            ToolTip="{x:Static languages:Core.ServiceTestInputValueTooltip}"
                                                                            AllowUserInsertLine="True"
                                                                            SelectAllOnGotFocus="True">
                                                        <AutomationProperties.AutomationId>
                                                            <MultiBinding StringFormat="UI_{0}txt_AutoID">
                                                                <Binding Path="Value"/>
                                                            </MultiBinding>
                                                        </AutomationProperties.AutomationId>
                                                        <ui:IntellisenseTextBox.IntellisenseProvider>
                                                            <interfaceImplementors:CompositeIntellisenseProvider>
                                                                <provider:BlankIntellisenseProvider />
                                                            </interfaceImplementors:CompositeIntellisenseProvider>
                                                        </ui:IntellisenseTextBox.IntellisenseProvider>
                                                    </ui:IntellisenseTextBox>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Width="*"
                                                                MinWidth="125"
                                                                Header="EmptyIsNull"
                                                                HeaderStyle="{StaticResource ActivityMapppingDataGridHeaderStyle}"
                                                                d:DataContext="{d:DesignInstance Type=interfaces:IServiceTestInput}">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding Path=EmptyIsNull, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                              HorizontalAlignment="Center"
                                                                  Checked="ToggleButton_OnChecked"
                                                                  Unchecked="ToggleButton_OnChecked"
                                                              ToolTip="{x:Static languages:Core.InputEmptyIsNullToolTip}">
                                                        <i:Interaction.Behaviors>
                                                            <core:HelpSetter Text="{x:Static languages:Core.InputEmptyIsNullDefaultValueHelp}" DataContext="{Binding}" />
                                                        </i:Interaction.Behaviors>
                                                    </CheckBox>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </controls:Dev2DataGrid>
                            </Border>
                            <!-- Grid.Row="2" is in place for Assert - V3 -->

                            <TextBlock Grid.Row="3" Text="Outputs" FontWeight="Bold"/>
                            <Border Grid.Row="4" BorderBrush="{StaticResource XamGridBorderBrush}" BorderThickness="1">
                                <controls:Dev2DataGrid ItemsSource="{Binding SelectedServiceTest.Outputs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                       AutoGenerateColumns="False"
                                                       BorderThickness="0"      
                                                       Style="{StaticResource ActivityMapppingDataGridStyle}"
                                                       x:Name="TestOutputsGrid">

                                    <!-- DO NOT override ItemsPanel - doing so causes column sizing issues on first drop -->
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Width="*"
                                                                MinWidth="125"
                                                                Header="Variable"
                                                                HeaderStyle="{StaticResource ActivityMapppingDataGridHeaderStyle}"
                                                                d:DataContext="{d:DesignInstance Type=interfaces:IServiceTestOutput}">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock KeyboardNavigation.IsTabStop="False" 
                                                               Focusable="True" 
                                                               ToolTip="{Binding Path=Variable}" 
                                                               Text="{Binding Path=Variable, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Width="*"
                                                                MinWidth="125"
                                                                Header="Value"
                                                                HeaderStyle="{StaticResource ActivityMapppingDataGridHeaderStyle}"
                                                                d:DataContext="{d:DesignInstance Type=interfaces:IServiceTestOutput}">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ui:IntellisenseTextBox FilterType="All" 
                                                                            WrapInBrackets="False" 
                                                                            Text="{Binding Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                                            Margin="1,0,1,1"
                                                                            AllowMultilinePaste="True"
                                                                            TextWrapping="Wrap"
                                                                             TextChanged="TxtValue_OnTextChanged"
                                                                            Focusable="True" 
                                                                            IsTabStop="True"
                                                                            AcceptsReturn="True"
                                                                            ToolTip="{x:Static languages:Core.ServiceTestInputValueTooltip}"
                                                                            AllowUserInsertLine="True"
                                                                            SelectAllOnGotFocus="True">
                                                        <AutomationProperties.AutomationId>
                                                            <MultiBinding StringFormat="UI_{0}txt_AutoID">
                                                                <Binding Path="Value"/>
                                                            </MultiBinding>
                                                        </AutomationProperties.AutomationId>
                                                        <ui:IntellisenseTextBox.IntellisenseProvider>
                                                            <interfaceImplementors:CompositeIntellisenseProvider>
                                                                <provider:BlankIntellisenseProvider />
                                                            </interfaceImplementors:CompositeIntellisenseProvider>
                                                        </ui:IntellisenseTextBox.IntellisenseProvider>
                                                    </ui:IntellisenseTextBox>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </controls:Dev2DataGrid>
                            </Border>

                            <Grid Grid.Row="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="{Binding ElementName=RunAsUserTextBlock, Path=ActualWidth}"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0" Text="Error" FontWeight="Bold"/>
                                <RadioButton x:Name="NoErrorRadioButton"
                                             Grid.Column="1"
                                             Padding="0,0,5,0" 
                                             ToolTip="{x:Static languages:Core.ServiceTestNoErrorExpectedTooltip}" 
                                             VerticalAlignment="Center" 
                                             HorizontalAlignment="Left" 
                                             IsChecked="{Binding SelectedServiceTest.NoErrorExpected, Mode=TwoWay}"
                                             Content="No Error">
                                    <i:Interaction.Behaviors>
                                        <core:HelpSetter Text="{x:Static languages:Core.ServiceTestNoErrorExpectedHelpText}" DataContext="{Binding}" />
                                    </i:Interaction.Behaviors>
                                </RadioButton>
                                <RadioButton x:Name="ErrorRadioButton" 
                                             Grid.Column="2"
                                             ToolTip="{x:Static languages:Core.ServiceTestErrorExpectedTooltip}" 
                                             VerticalAlignment="Center" 
                                             HorizontalAlignment="Left" 
                                             IsChecked="{Binding SelectedServiceTest.ErrorExpected, Mode=TwoWay}" 
                                             Content="Error">
                                    <i:Interaction.Behaviors>
                                        <core:HelpSetter Text="{x:Static languages:Core.ServiceTestErrorExpectedHelpText}" DataContext="{Binding}" />
                                    </i:Interaction.Behaviors>
                                </RadioButton>
                            </Grid>
                        </Grid>
                    </Grid>
                </ContentControl>
            </Grid>
        </Border>
    </Grid>
</UserControl>
