import { ElementRef, Renderer2 } from '@angular/core';
export declare class BaseFxDirective {
    private elRef;
    private renderer;
    private prevValueCache;
    constructor(elRef: ElementRef, renderer: Renderer2);
    protected removeClass(className: any): void;
    protected setClass(attr: string, value?: string): void;
    private normalize(value);
}
