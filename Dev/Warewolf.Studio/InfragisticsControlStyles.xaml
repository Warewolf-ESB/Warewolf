<Window x:Class="Warewolf.Studio.InfragisticsControlStyles"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:core="clr-namespace:Warewolf.Studio.Core;assembly=Warewolf.Studio.Core"
        xmlns:ig="http://schemas.infragistics.com/xaml"
        Title="InfragisticsControlStyles" Height="800" Width="600">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <core:ResourceTypeToIconConverter x:Key="ResourceTypeToIconConverter" />
    </Window.Resources>
    <WrapPanel Orientation="Vertical" Margin="{StaticResource ContainerBorder}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <ig:XamGrid Grid.Row="0">
                
            </ig:XamGrid>

            <ig:XamComboEditor x:Name="ComboEditor"
                               Grid.Row="1"
                               ItemsSource="{Binding DataItem.Cities}"
                               IsEditable="True"
                               AllowFiltering="True"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"
                               DisplayMemberPath="Company"
                               Height="30" Width="300" />

            <!--<igDP:XamDataGrid DataSource="{Binding}" Name="xamDataGrid1" VerticalAlignment="Top" >
                <igDP:XamDataGrid.FieldLayoutSettings>
                    <igDP:FieldLayoutSettings AutoGenerateFields="False" />
                </igDP:XamDataGrid.FieldLayoutSettings>
                <igDP:XamDataGrid.FieldLayouts>
                    <igDP:FieldLayout>
                        <igDP:Field Name="Country"/>
                        <igDP:UnboundField Name="Cities" Label="Cities" Width="Auto">
                            <igDP:UnboundField.Settings>
                                <igDP:FieldSettings>
                                    <igDP:FieldSettings.EditorStyle>
                                        <Style TargetType="{x:Type ig:XamComboEditor}">
                                            <Style.Setters>
                                                <Setter Property="ItemsSource" Value="{Binding DataItem.Cities}" />
                                            </Style.Setters>
                                        </Style>
                                    </igDP:FieldSettings.EditorStyle>
                                </igDP:FieldSettings>
                            </igDP:UnboundField.Settings>
                        </igDP:UnboundField>
                    </igDP:FieldLayout>
                </igDP:XamDataGrid.FieldLayouts>
            </igDP:XamDataGrid>-->

        </Grid>
    </WrapPanel>
</Window>
