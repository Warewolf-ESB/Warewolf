<Window x:Class="Warewolf.Studio.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:core="clr-namespace:Warewolf.Studio.Core;assembly=Warewolf.Studio.Core"
        xmlns:dockManager="http://infragistics.com/DockManager"
        xmlns:regions="http://www.codeplex.com/CompositeWPF"
        x:Name="MainWindow"
        Title="Warewolf Studio"
        WindowState="Maximized">

    <Window.Resources>


        <DataTemplate x:Key="HelpPaneHeaderTemplate">
            <Border Background="#FFAEACAC">
                <TextBlock>WHAT DOES THIS DO?</TextBlock>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <dockManager:XamDockManager x:Name="DockManager"
                                            AllowMaximizeFloatingWindows="True"
                                            AllowMinimizeFloatingWindows="True"
                                            FlyoutAnimation="Resize"
                                            LayoutMode="FillContainer"
                                            PaneDragStarting="DockManager_OnPaneDragStarting"
                                            UnpinnedTabHoverAction="Flyout">
                    <dockManager:XamDockManager.Panes>

                        <dockManager:SplitPane Width="220"
                                               SplitterOrientation="Horizontal"
                                               dockManager:XamDockManager.InitialLocation="DockedLeft">

                            <dockManager:ContentPane x:Name="Menu"
                                                     AllowClose="False"
                                                     AllowDocking="False"
                                                     AllowFloatingOnly="False"
                                                     AllowInDocumentHost="False"
                                                     CloseButtonVisibility="Hidden"
                                                     Header="MENU"
                                                     IsPinned="True"
                                                     PinButtonVisibility="Hidden"
                                                     Style="{DynamicResource DockmangerMenuPaneStyle}"
                                                     WindowPositionMenuVisibility="Hidden">
                                <StackPanel Orientation="Vertical">
                                    <Button Content="New" Style="{DynamicResource MainMenuButtonStyle}" />
                                    <Button Content="Save" Style="{DynamicResource MainMenuButtonStyle}" />
                                    <Button BorderThickness="0"
                                            Content="Deploy"
                                            Style="{DynamicResource MainMenuButtonStyle}" />
                                    <Separator />
                                    <Button Content="Db Service" Style="{DynamicResource MainMenuButtonStyle}" />
                                    <Button Content="Plugin Service" Style="{DynamicResource MainMenuButtonStyle}" />
                                    <Button Content="Web Service" Style="{DynamicResource MainMenuButtonStyle}" />
                                    <Separator />
                                    <Button Content="Schedule" Style="{DynamicResource MainMenuButtonStyle}" />
                                    <Button Content="Debug" Style="{DynamicResource MainMenuButtonStyle}" />
                                    <Separator />
                                    <Button Content="Settings" Style="{DynamicResource MainMenuButtonStyle}" />
                                    <Separator />
                                    <Button Content="Help" Style="{DynamicResource MainMenuButtonStyle}" />
                                    <Separator />
                                    <Button VerticalAlignment="Bottom"
                                            HorizontalContentAlignment="Center"
                                            VerticalContentAlignment="Bottom"
                                            Content="Pin"
                                            Style="{DynamicResource MainMenuButtonStyle}" />
                                </StackPanel>

                            </dockManager:ContentPane>
                        </dockManager:SplitPane>

                        <dockManager:SplitPane Width="300"
                                               SplitterOrientation="Horizontal"
                                               dockManager:XamDockManager.InitialLocation="DockedRight">


                            <dockManager:ContentPane x:Name="Variables"
                                                     AllowClose="False"
                                                     AllowInDocumentHost="False"
                                                     CloseButtonVisibility="Hidden"
                                                     Header="VARIABLES"
                                                     IsPinned="True"
                                                     Style="{DynamicResource DockmangerToolPaneStyle}"
                                                     WindowPositionMenuVisibility="Hidden"
                                                     regions:RegionManager.RegionName="Variables" />

                            <dockManager:ContentPane x:Name="OutputPane"
                                                     AllowClose="False"
                                                     AllowInDocumentHost="False"
                                                     CloseButtonVisibility="Hidden"
                                                     Header="OUTPUT"
                                                     IsPinned="True"
                                                     Style="{DynamicResource DockmangerToolPaneStyle}"
                                                     WindowPositionMenuVisibility="Hidden"
                                                     regions:RegionManager.RegionName="OutputPane" />

                        </dockManager:SplitPane>

                        <dockManager:SplitPane Width="300"
                                               SplitterOrientation="Horizontal"
                                               dockManager:XamDockManager.InitialLocation="DockedLeft">
                            <dockManager:ContentPane x:Name="Explorer"
                                                     AllowClose="False"
                                                     AllowInDocumentHost="False"
                                                     CloseButtonVisibility="Hidden"
                                                     Header="EXPLORER"
                                                     IsPinned="True"
                                                     Style="{DynamicResource DockmangerToolPaneStyle}"
                                                     WindowPositionMenuVisibility="Hidden"
                                                     regions:RegionManager.RegionName="{x:Static core:RegionNames.Explorer}" />
                            <dockManager:ContentPane x:Name="Toolbox"
                                                     AllowInDocumentHost="False"
                                                     CloseButtonVisibility="Hidden"
                                                     Header="TOOL BOX"
                                                     IsPinned="True"
                                                     Style="{DynamicResource DockmangerToolPaneStyle}"
                                                     WindowPositionMenuVisibility="Hidden"
                                                     regions:RegionManager.RegionName="{x:Static core:RegionNames.Toolbox}" />
                            <dockManager:ContentPane x:Name="Help"
                                                     Height="400"
                                                     AllowClose="False"
                                                     AllowDocking="False"
                                                     AllowFloatingOnly="False"
                                                     AllowInDocumentHost="False"
                                                     CloseButtonVisibility="Hidden"
                                                     Header="WHAT DOES THIS DO?"
                                                     IsPinned="True"
                                                     PinButtonVisibility="Hidden"
                                                     Style="{DynamicResource DockmangerToolPaneStyle}"
                                                     WindowPositionMenuVisibility="Hidden"
                                                     regions:RegionManager.RegionName="Help" />
                        </dockManager:SplitPane>
                    </dockManager:XamDockManager.Panes>
                    <dockManager:DocumentContentHost>
                        <dockManager:SplitPane>
                            <dockManager:TabGroupPane x:Name="_workflows" regions:RegionManager.RegionName="{x:Static core:RegionNames.Workflows}" />
                        </dockManager:SplitPane>
                    </dockManager:DocumentContentHost>
                </dockManager:XamDockManager>
            </Grid>
        </Grid>
    </Grid>
</Window>
