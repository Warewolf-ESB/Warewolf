<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dockManager="http://infragistics.com/DockManager"
        xmlns:regions="http://www.codeplex.com/CompositeWPF"
        xmlns:core="clr-namespace:Warewolf.Studio.Core;assembly=Warewolf.Studio.Core"
    x:Class="Warewolf.Studio.Shell"
        Title="Warewolf Studio" 
        x:Name="MainWindow"
        WindowState="Maximized">

    <Window.Resources>


        <DataTemplate x:Key="HelpPaneHeaderTemplate">
            <Border Background="#FFAEACAC">
                <TextBlock>WHAT DOES THIS DO?</TextBlock>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <dockManager:XamDockManager PaneDragStarting="DockManager_OnPaneDragStarting" LayoutMode="FillContainer" UnpinnedTabHoverAction="Flyout" FlyoutAnimation="Resize" AllowMaximizeFloatingWindows="True" AllowMinimizeFloatingWindows="True" x:Name="DockManager">
                    <dockManager:XamDockManager.Panes>

                        <dockManager:SplitPane SplitterOrientation="Horizontal" Width="220" dockManager:XamDockManager.InitialLocation="DockedLeft">

                            <dockManager:ContentPane Header="MENU" x:Name="Menu"
								CloseButtonVisibility="Hidden"
								WindowPositionMenuVisibility="Hidden"
								PinButtonVisibility="Hidden"
								AllowInDocumentHost="False"
								AllowDocking="False"
								AllowFloatingOnly="False"
                                AllowClose="False"
                                IsPinned="True"
                                                     >
                                <StackPanel Orientation="Vertical">
                                    <Button Style="{DynamicResource MenuButtonStyle}" Content="New"/>
                                    <Button Content="Save" Style="{DynamicResource MenuButtonStyle}"/>
                                    <Button Content="Deploy" Style="{DynamicResource MenuButtonStyle}" BorderThickness="0"/>
                                    <Separator/>
                                    <Button Content="Db Service" Style="{DynamicResource MenuButtonStyle}"/>
                                    <Button Content="Plugin Service" Style="{DynamicResource MenuButtonStyle}"/>
                                    <Button Content="Web Service" Style="{DynamicResource MenuButtonStyle}"/>
                                    <Separator/>
                                    <Button Content="Schedule" Style="{DynamicResource MenuButtonStyle}"/>
                                    <Button Content="Debug" Style="{DynamicResource MenuButtonStyle}"/>
                                    <Separator/>
                                    <Button Content="Settings" Style="{DynamicResource MenuButtonStyle}"/>
                                    <Separator/>
                                    <Button Content="Help" Style="{DynamicResource MenuButtonStyle}"/>
                                    <Separator/>
                                    <Button VerticalContentAlignment="Bottom" VerticalAlignment="Bottom" 
                                            HorizontalContentAlignment="Center" 
                                            Content="Pin" 
                                            Style="{DynamicResource MenuButtonStyle}"/>
                                </StackPanel>

                            </dockManager:ContentPane>
                        </dockManager:SplitPane>

                        <dockManager:SplitPane Width="300"
							SplitterOrientation="Horizontal"
							dockManager:XamDockManager.InitialLocation="DockedRight">


                            <dockManager:ContentPane x:Name="Variables"
                                WindowPositionMenuVisibility="Hidden"
								CloseButtonVisibility="Hidden"
								AllowClose="False"
								AllowInDocumentHost="False"
								Header="Variables"
								IsPinned="True"
								regions:RegionManager.RegionName="Variables" />

                            <dockManager:ContentPane x:Name="OutputPane"
								AllowClose="False"
                                WindowPositionMenuVisibility="Hidden"
								CloseButtonVisibility="Hidden"
								AllowInDocumentHost="False"
								Header="Output"
								IsPinned="True"
								regions:RegionManager.RegionName="OutputPane" />

                        </dockManager:SplitPane>

                        <dockManager:SplitPane Width="300"
							SplitterOrientation="Horizontal"
							dockManager:XamDockManager.InitialLocation="DockedLeft">
                            <dockManager:ContentPane x:Name="Explorer"
								AllowClose="False"
								WindowPositionMenuVisibility="Hidden"
								CloseButtonVisibility="Hidden"
								AllowInDocumentHost="False"
								Header="Explorer"
								IsPinned="True"
								regions:RegionManager.RegionName="{x:Static core:RegionNames.Explorer}" />
                            <dockManager:ContentPane x:Name="Toolbox"
								AllowInDocumentHost="False"
								Header="Toolbox" 
                                WindowPositionMenuVisibility="Hidden"
								CloseButtonVisibility="Hidden"
                                IsPinned="True"
								regions:RegionManager.RegionName="{x:Static core:RegionNames.Toolbox}" />
                            <dockManager:ContentPane x:Name="Help"
								HeaderTemplate="{StaticResource HelpPaneHeaderTemplate}"
                                AllowDocking="False"
								AllowFloatingOnly="False"
                                AllowClose="False"
                                AllowInDocumentHost="False"
								WindowPositionMenuVisibility="Hidden"
								CloseButtonVisibility="Hidden"
                                PinButtonVisibility="Hidden"
								IsPinned="True"
                                Height="400"
								regions:RegionManager.RegionName="Help" />
                        </dockManager:SplitPane>
                    </dockManager:XamDockManager.Panes>
                    <dockManager:DocumentContentHost>
                        <dockManager:SplitPane>
                            <dockManager:TabGroupPane x:Name="_workflows" regions:RegionManager.RegionName="{x:Static core:RegionNames.Workflows}" />
                        </dockManager:SplitPane>
                    </dockManager:DocumentContentHost>
                </dockManager:XamDockManager>
            </Grid>
        </Grid>
    </Grid>
</Window>
