<ss:ModernActionStep x:Class="Gui.LicenseStep"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:ss="http://sharpsetup.eu/schemas/xaml/wpfui/2012"
                     xmlns:props="clr-namespace:Gui.Properties"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     mc:Ignorable="d"
                     d:DesignHeight="360"
                     d:DesignWidth="500"
                     Title="{x:Static props:Resources.LicenseStep_Title}"
                     Subtitle="{x:Static props:Resources.LicenseStep_Subtitle}"
                     CanGoNext="False"
                     Style="{StaticResource {x:Type ss:ModernActionStep}}">
    <DockPanel>
        <Grid Background="Transparent"
              DockPanel.Dock="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="35" />
                <RowDefinition Height="1" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Style="{StaticResource TextBlockStyle}"
                       Background="White"
                       Padding="20,10,10,10"
                       Height="35"
                       Grid.Row="0"
                       Grid.Column="0"
                       FontSize="14"
                       FontWeight="Bold"
                       TextWrapping="Wrap"
                       Text="{Binding Title,RelativeSource={RelativeSource FindAncestor,AncestorType=ss:ModernActionStep}}" />
            <Rectangle Grid.Row="1"
                       Fill="#F3700D"></Rectangle>
            <Border Grid.Row="2"
                    Padding="10"
                    Background="White">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock TextWrapping="Wrap"
                               Height="25"
                               Grid.Row="0"
                               Text="{Binding Subtitle,RelativeSource={RelativeSource FindAncestor,AncestorType=ss:ModernActionStep}}" />
                    <RichTextBox Name="rtbLicense"
                                 Grid.Row="1"
                                 Margin="0,5,0,15"
                                 BorderBrush="#7F7F7F"
                                 SelectionBrush="#7F7F7F"
                                 IsReadOnly="True"
                                 FontFamily="{StaticResource DefaultFontFamily}"
                                 Background="#D4D5D6"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto" />
                    <CheckBox Grid.Row="2"
                              FontWeight="Bold"
                              Style="{StaticResource CheckBoxStyle}"
                              Name="cbAccept"
                              IsChecked="{Binding CanGoNext, RelativeSource={RelativeSource FindAncestor, AncestorType=ss:ModernActionStep}}"
                              Content="{x:Static props:Resources.LicenseStep_AcceptLicense}"
                              Margin="0,0,0,5" />
                </Grid>
            </Border>
        </Grid>

    </DockPanel>
</ss:ModernActionStep>
