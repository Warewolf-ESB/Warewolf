<UserControl x:Class="Unlimited.Applications.BusinessDesignStudio.WebsiteLayoutObjectUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel MouseDown="StackPanel_MouseDown" x:Name="cellView">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter Property="Background" Value="LightBlue" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsSelected}" Value="False">
                            <Setter Property="Background" Value="LightGray" />
                        </DataTrigger>
                    </Style.Triggers>
                <Style.Setters>
                    <Setter Property="AutomationProperties.AutomationId">
                        <Setter.Value>
                            <MultiBinding StringFormat="UI_{0}_{1}_AutoID" >
                                
                            </MultiBinding>
                        </Setter.Value>
                    </Setter>
                </Style.Setters>
            </Style>
            </StackPanel.Style>
            <Grid IsHitTestVisible="False">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="35" />
                    <RowDefinition Height="35"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Image Width="35" Height="35" Margin="3,0" Source="{Binding IconPath, Converter={StaticResource ImagePathConverter}}" />
                <TextBlock Text="{Binding WebpartServiceDisplayName}" MaxWidth="110" MaxHeight="35" Grid.Column="1" FontWeight="Bold" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" FontSize="13" />
                <TextBlock Grid.ColumnSpan="2" Grid.Row="1" MaxWidth="110" MaxHeight="35" Text="{Binding DisplayName}" HorizontalAlignment="Center" TextWrapping="Wrap" TextTrimming="CharacterEllipsis">

                </TextBlock>
            </Grid>
        </StackPanel>
</UserControl>
